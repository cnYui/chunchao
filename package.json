{
  "name": "chunchao-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 8002",
    "build": "vite build",
    "test": "node --no-warnings --test scripts/**/*.test.mjs"
  },
  "dependencies": {
    "@strudel/web": "^1.3.0"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  }
}
