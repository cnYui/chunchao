# GitHub 发布准备记录

## 本次目标

- 把当前静态场景项目整理为可发布的 Git 仓库
- 为后续推送到 GitHub 补齐最小仓库配置
- 记录发布过程中的约束和阻塞点

## 当前项目状态

- 项目目录原先不是 Git 仓库
- 当前代码以静态页面为主，核心文件是 `index.html`、`styles.css`、`main.js` 和 `pic/`
- `docs/ai/context/` 已存在，可继续沉淀发布相关上下文

## 本次发布决策

- 保留现有静态项目结构，不为发布额外引入构建链路
- 图片素材直接纳入版本管理，原因是它们就是场景运行所必需的主资源
- 忽略 `.codex-logs/`，原因是其中是本地运行日志，不属于项目源码
- 新增 `.gitattributes` 固定文本文件为 `LF`，原因是避免 Windows 本地与 GitHub 仓库之间出现无意义换行差异
- 先完成本地 Git 初始化和首个提交，再处理 GitHub 远程创建与推送

## 当前阻塞

- 本机 `gh` 可执行，但当前 GitHub 账号 `cnYui` 的令牌已失效
- 已改为直接绑定现成远程仓库地址完成首次推送，不再依赖 `gh` 建仓

## 结果

- 远程仓库已绑定为 `https://github.com/cnYui/chunchao.git`
- 本地分支 `main` 已推送并跟踪 `origin/main`
- 首次推送包含静态场景代码、素材资源和现有上下文文档
- 仓库首页补充 `README.md`，定位为对外快速说明，不重复堆叠内部设计细节

## 建议后的衔接方式

1. 后续若要用 `gh` 管理仓库，再补一次 `gh auth login`
2. 日常直接使用 `git add`、`git commit`、`git push` 即可
