# 贴纸素材记录

## 当前状态

- `pic/1.png` 到 `pic/16.png` 当前作为 16 张独立贴纸素材保留。
- 这 16 张图已不再使用自动裁切结果，而是用用户提供的现成贴纸文件按同名编号覆盖。
- 源文件目录为 `D:\xwechat_files\wxid_4lkns2swsaad22_1df8\msg\file\2026-04\贴纸\贴纸\`。
- 当前预览拼图已生成在 `.codex-logs/stickers-1-16-preview.png`，仅用于本地核对，不作为运行素材。

## 编号规则

- 项目内运行路径固定为 `pic/{编号}.png`。
- 编号范围为 `1.png` 到 `16.png`。
- 后续如果要在前端使用贴纸，优先直接读取 `pic/1.png` 到 `pic/16.png`，不要再从合图重新裁切。
